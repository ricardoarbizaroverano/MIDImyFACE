# relay server — Node.js artefacts (never commit these)
relay/node_modules/
relay/package-lock.json
relay/.env.local
relay/.env.*.local
relay/*.log

# macOS
.DS_Store

# local-only notes
.sandbox-notes.md
