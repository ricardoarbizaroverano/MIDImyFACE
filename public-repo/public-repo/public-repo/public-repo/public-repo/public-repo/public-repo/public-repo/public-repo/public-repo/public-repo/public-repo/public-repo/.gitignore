# relay server — Node.js artefacts (never commit these)
relay/node_modules/
relay/package-lock.json
relay/.env.local
relay/.env.*.local
relay/*.log

# ── Encryption secrets — NEVER commit ─────────────────────────────────────
.env
.env.*
*.secret
MMF_SECRET_KEY

# ── Readable source lives in src/ — committed to PRIVATE repo only.
# The public repo only ever receives the .enc encrypted output.
# Do NOT add src/ here — it needs to be committed to the private repo.
# The GitHub Action excludes it from the public deployment automatically.

# macOS
.DS_Store

# local-only notes
.sandbox-notes.md
relay/.env
